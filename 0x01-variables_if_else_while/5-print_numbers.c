#include <stdio.h>

/**
 * main - Executor
 * Description: Print numbers
 * Return: 0
 */

int main(void)
{
	int a = 0;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 * main - executor
 * Description: The use of printf
 * Return: 0
 */

int main(void)
{
char print[] = "with proper grammar, but the outcome is a piece of art,\n";
printf(print);
return (0);
}

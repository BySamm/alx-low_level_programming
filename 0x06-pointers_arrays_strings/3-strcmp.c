#include "main.h"
#include <string.h>

/**
 * _strcmp - Function to compare two strings
 * @s1: First string
 * @s2: Second string
 * Return: result
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
